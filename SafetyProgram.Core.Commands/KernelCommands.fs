module KernelCommands

open System
open SafetyProgram.Core.IO.Services
open SafetyProgram.Core.Models
open SafetyProgram.Core
open System.Xml.Linq

let closeDocument kernelData (service : DataService<Document>) = 
    new NotImplementedException() |> raise

let newDocument a =
    new NotImplementedException() |> raise

let openDocument a = 
    new NotImplementedException() |> raise

let redo a = 
    new NotImplementedException() |> raise

let undo a = 
    new NotImplementedException() |> raise

let save a =
    new NotImplementedException() |> raise

let saveAs a =
    new NotImplementedException() |> raise

