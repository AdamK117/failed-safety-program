<Fluent:RibbonWindow x:Class="SafetyProgram.CoshhWindowView"
        xmlns:Fluent="clr-namespace:Fluent;assembly=Fluent"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Document.Title}" Height="600" Width="800">
    
    <Grid x:Name="LayoutRoot">
        
        <!-- Grid Definitions -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <!-- Ribbon -->
        <ContentControl Content="{Binding Ribbon.View}" Grid.Row="0" />
        
        <!-- Document -->
        <ContentControl Content="{Binding Document.View}" Grid.Row="1" />

    </Grid>
</Fluent:RibbonWindow>
