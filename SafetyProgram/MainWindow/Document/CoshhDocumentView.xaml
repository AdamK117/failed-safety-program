<UserControl x:Class="SafetyProgram.MainWindow.Document.CoshhDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <ScrollViewer x:Name="DocumentScroll">
        
        <StackPanel x:Name="LayoutRoot" Width="{Binding Format.Width}" ContextMenu="{Binding ContextMenu.View}" PreviewMouseLeftButtonDown="DocumentClicked" Background="White">
            
            <ItemsControl ItemsSource="{Binding Body}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding View}" PreviewMouseLeftButtonDown="DocObjectClicked" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
        </StackPanel>
        
    </ScrollViewer>
    
</UserControl>