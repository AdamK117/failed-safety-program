<UserControl x:Class="SafetyProgram.UI.Document.DocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <ScrollViewer x:Name="DocumentWrapper" Focusable="False">
        <StackPanel x:Name="DocumentHolder" Width="{Binding Format.Width}" ContextMenu="{Binding ContextMenu.View}"  Background="White">
            
            <ItemsControl ItemsSource="{Binding DocumentObjects}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                    <ContentControl Content="{Binding View}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
        </StackPanel>
    </ScrollViewer>

</UserControl>